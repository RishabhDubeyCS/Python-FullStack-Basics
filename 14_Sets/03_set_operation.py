a = {3,23,1}
b = {23, 4,2,55,1}
c = a.union(b) # The union of two set a , b  denoted as a union b includes all element that are in either a,b or both 
print(c)
d = a.intersection(b)
print(d)   # The intersection of twon set a,b  (denoted as a intersection b)include only the element that are common to both and b
e  = a.difference(b)
print(e)
# Use Case Set are great for eliminating duplicate values
