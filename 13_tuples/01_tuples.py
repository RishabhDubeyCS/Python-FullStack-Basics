# Tuple and Operation on tuples 
# Tuple are ordered  but immutable collection (connot be changed after creation )
# Creating a Tuple 
my_tuple = (3,5,6,7,8,4)
print(my_tuple , type(my_tuple))
single_element =(2,) # Tuple with one element (comma required)
# Accessing Tuple Element 
print(my_tuple[2])
